{
  "name": "allma.back-end-exam-events",
  "version": "1.0.0",
  "description": "Allma back-end engineer take-home interview challenge",
  "main": "app.js",
  "scripts": {
    "start": "npm run serve",
    "debug": "concurrently -k -p \"[{name}]\" -n \"TypeScript,      Node\" -c \"magenta.bold,cyan.bold\" \"npm run build-watch\" \"npm run serve-watch\"",
    "serve": "node dist/app.js",
    "serve-watch": "nodemon --inspect dist/app.js",
    "build": "tsc",
    "build-watch": "tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "dependencies": {
    "cross-fetch": "^3.0.6",
    "dotenv": "^8.2.0",
    "typescript": "^4.1.3",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@types/node": "^14.14.25",
    "@types/uuid": "^8.3.0",
    "concurrently": "^5.3.0",
    "nodemon": "^2.0.7"
  }
}
